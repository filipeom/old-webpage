<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://www.filipeom.dev/feed.xml" rel="self" type="application/atom+xml"/><link href="https://www.filipeom.dev/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-10-01T11:13:24+00:00</updated><id>https://www.filipeom.dev/feed.xml</id><title type="html">blank</title><entry><title type="html">Rust 101</title><link href="https://www.filipeom.dev/blog/2023/rust101/" rel="alternate" type="text/html" title="Rust 101"/><published>2023-11-16T00:00:00+00:00</published><updated>2023-11-16T00:00:00+00:00</updated><id>https://www.filipeom.dev/blog/2023/rust101</id><content type="html" xml:base="https://www.filipeom.dev/blog/2023/rust101/"><![CDATA[<h1 id="prelude">Prelude</h1> <p>This script was made for the 2023/2024 Analysis and Synthesis of Algorithms course at Instituto Superior Técnico.</p> <h1 id="getting-started">Getting Started</h1> <p>To install rust, please, follow the official documentation.</p> <ul> <li><a href="https://www.rust-lang.org/tools/install">https://www.rust-lang.org/tools/install</a></li> </ul> <p>You should, at least, have the rust compiler (i.e., <code class="language-plaintext highlighter-rouge">rustc</code>) installed.</p> <h1 id="managing-rust-projects-with-cargo">Managing Rust Projects with Cargo</h1> <h2 id="cargo-overview">Cargo Overview</h2> <p><code class="language-plaintext highlighter-rouge">cargo</code> is Rust’s official build tool and package manager:</p> <ul> <li>Simplifies project management, dependency handling, and building.</li> </ul> <p>Usage examples:</p> <p>\small</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cargo new my_project    <span class="c"># create a new project</span>
cargo build             <span class="c"># build project</span>
cargo build <span class="nt">--profile</span> realease <span class="c"># builds optimized binary</span>
cargo run               <span class="c"># runs the project</span>
cargo <span class="nb">test</span>              <span class="c"># runs tests</span>
</code></pre></div></div> <h1 id="managing-rust-projects-with-cargo-1">Managing Rust Projects with Cargo</h1> <h2 id="benefits-of-cargo">Benefits of Cargo</h2> <p>Dependency Management and Build Automation:</p> <ul> <li>Automatically fetches and manages project dependencies.</li> <li>Handles compilation, linking, and building release artifacts.</li> </ul> <p>Project Structure:</p> <ul> <li>Enforces a conventional project structure for consistency.</li> </ul> <p><code class="language-plaintext highlighter-rouge">rustc</code>: The Rust compiler.</p> <ul> <li>Can be used directly, but Cargo simplifies many tasks.</li> </ul> <h1 id="control-flow-constructs-if-statement">Control Flow Constructs: If-statement</h1> <p>Rust provides flexible control flow constructs, including <code class="language-plaintext highlighter-rouge">if</code> statements, <code class="language-plaintext highlighter-rouge">for</code>-loops and <code class="language-plaintext highlighter-rouge">while</code>-loops.</p> <ul> <li>The <code class="language-plaintext highlighter-rouge">if</code> statement syntax is straightforward:</li> </ul> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">x</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span>
<span class="k">if</span> <span class="n">x</span> <span class="o">&gt;</span> <span class="mi">0</span> <span class="p">{</span>
    <span class="nd">println!</span><span class="p">(</span><span class="s">"Positive!"</span><span class="p">)</span>
<span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
    <span class="nd">println!</span><span class="p">(</span><span class="s">"Non-positive"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div> <h1 id="control-flow-constructs-loops">Control Flow Constructs: Loops</h1> <h2 id="while-loop">While loop</h2> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span> <span class="n">condition</span> <span class="p">{</span>
    <span class="c1">// Code inside the loop</span>
<span class="p">}</span>
</code></pre></div></div> <p>Example:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="k">mut</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">3</span><span class="p">;</span>
<span class="k">while</span> <span class="n">n</span> <span class="o">&gt;</span> <span class="mi">0</span> <span class="p">{</span>
    <span class="nd">println!</span><span class="p">(</span><span class="s">"Countdown: {}"</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
    <span class="n">n</span> <span class="o">-=</span> <span class="mi">1</span>
<span class="p">}</span>
</code></pre></div></div> <h1 id="control-flow-constructs-loops-1">Control Flow Constructs: Loops</h1> <h2 id="for-loop">For loop</h2> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">item</span> <span class="k">in</span> <span class="n">iterable</span> <span class="p">{</span>
    <span class="c1">// Code to run for each item</span>
<span class="p">}</span>
</code></pre></div></div> <p>Example:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">ns</span> <span class="o">=</span> <span class="nd">vec!</span><span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">6</span><span class="p">];</span>
<span class="k">for</span> <span class="n">n</span> <span class="k">in</span> <span class="n">ns</span> <span class="p">{</span>
    <span class="nd">println!</span><span class="p">(</span><span class="s">"Number: {}"</span><span class="p">,</span> <span class="n">n</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <h1 id="ranges-and-iterators">Ranges and Iterators</h1> <p>Rust has a powerful syntax to declare ranges over natural numbers:</p> <ul> <li>Write <code class="language-plaintext highlighter-rouge">0..n</code> to declare a range starting in $0$ to $n-1$, and</li> <li>Write <code class="language-plaintext highlighter-rouge">0..=n</code> to declare a range starting in $0$ to $n$</li> </ul> <p>Example:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">n</span> <span class="k">in</span> <span class="mi">1</span><span class="o">..</span><span class="mi">5</span> <span class="p">{</span>
    <span class="c1">// Code to run for each number in the range [1, 5[</span>
<span class="p">}</span>
<span class="k">for</span> <span class="n">n</span> <span class="k">in</span> <span class="mi">1</span><span class="o">..=</span><span class="mi">5</span> <span class="p">{</span>
    <span class="c1">// Code to run for each number in the range [1, 5]</span>
<span class="p">}</span>
</code></pre></div></div> <h1 id="ranges-and-iterators-1">Ranges and Iterators</h1> <p>Inspired from functional languages Rust allows us to write concise code using iterators. For example:</p> <ul> <li>One can obtain the sum of a vector by simply writing:</li> </ul> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">ns</span> <span class="o">=</span> <span class="nd">vec!</span><span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="p">];</span>
<span class="k">let</span> <span class="n">sum</span><span class="p">:</span> <span class="nb">i32</span> <span class="o">=</span> <span class="n">ns</span><span class="nf">.iter</span><span class="p">()</span><span class="nf">.sum</span><span class="p">();</span>
</code></pre></div></div> <ul> <li>Or, the squares of the previous vector with:</li> </ul> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">squared</span> <span class="o">=</span> <span class="n">ns</span><span class="nf">.iter</span><span class="p">()</span><span class="nf">.map</span><span class="p">(|</span><span class="n">x</span><span class="p">|</span> <span class="n">x</span> <span class="o">*</span> <span class="n">x</span><span class="p">)</span><span class="nf">.collect</span><span class="p">();</span>
</code></pre></div></div> <p>Note that, <code class="language-plaintext highlighter-rouge">|x| x * x</code> represents a <em>lambda</em>/<em>closure</em>.</p> <h1 id="closures">Closures</h1> <p>A <em>lambda</em> is an anonymous function used to express single operations or behaviours.</p> <ul> <li>In Rust, lambdas are called <em>closures</em></li> <li>Closures are defined using the <code class="language-plaintext highlighter-rouge">|param| expr</code> syntax</li> </ul> <p>In the previous example, <code class="language-plaintext highlighter-rouge">|x| x * x</code> is a closure that takes a single parameter <code class="language-plaintext highlighter-rouge">x</code> and returns the square of <code class="language-plaintext highlighter-rouge">x</code> (i.e., <code class="language-plaintext highlighter-rouge">x * x</code>). Typically, the use of closures is recommend in scenarios where a short, one-off function is needed, and there’s no need to define a separate function.</p> <h1 id="pattern-matching">Pattern Matching</h1> <p>Rust also has a powerful pattern matching construct:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">match</span> <span class="n">value</span> <span class="p">{</span>
    <span class="n">pattern1</span> <span class="k">=&gt;</span> <span class="c1">// Code for pattern1,</span>
    <span class="n">pattern2</span> <span class="k">if</span> <span class="n">condition</span> <span class="k">=&gt;</span> <span class="c1">// Code for pattern2,</span>
    <span class="n">_</span> <span class="k">=&gt;</span> <span class="c1">// Default code,</span>
<span class="p">}</span>
</code></pre></div></div> <p>For example:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">result</span> <span class="o">=</span> <span class="k">match</span> <span class="n">x</span> <span class="p">{</span>
    <span class="mi">1</span> <span class="k">=&gt;</span> <span class="s">"One"</span><span class="p">,</span>
    <span class="mi">2</span> <span class="p">|</span> <span class="mi">3</span> <span class="k">=&gt;</span> <span class="s">"Two or Three"</span><span class="p">,</span>
    <span class="n">x</span> <span class="k">if</span> <span class="n">x</span> <span class="o">&lt;</span> <span class="mi">0</span> <span class="k">=&gt;</span> <span class="s">"Negative"</span><span class="p">,</span>
    <span class="n">_</span> <span class="k">=&gt;</span> <span class="s">"Other"</span><span class="p">,</span>
<span class="p">};</span>
</code></pre></div></div> <h1 id="option-and-result">Option and Result</h1> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">pub</span> <span class="k">enum</span> <span class="nb">Option</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="p">{</span>       <span class="k">pub</span> <span class="k">enum</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="n">T</span><span class="p">,</span> <span class="n">E</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="nb">None</span><span class="p">,</span>                      <span class="nf">Ok</span><span class="p">(</span><span class="n">T</span><span class="p">),</span>
    <span class="nf">Some</span><span class="p">(</span><span class="n">T</span><span class="p">),</span>                   <span class="nf">Err</span><span class="p">(</span><span class="n">E</span><span class="p">),</span>
<span class="p">}</span>                          <span class="p">}</span>
</code></pre></div></div> <p><a href="https://doc.rust-lang.org/std/option/enum.Option.html"><strong>Option</strong></a> and <a href="https://doc.rust-lang.org/std/result/enum.Result.html"><strong>Result</strong></a> are algebraic data types (ADTs) in Rust, providing a concise and explicit approach to managing potential missing values or errors in functions, thus eliminating implicit exceptions. This forces developers to actively acknowledge and handle these scenarios, promoting robust error management and contributing to enhanced code clarity and safety.</p> <h1 id="option-and-result-1">Option and Result</h1> <p>Examples:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">maybe_value</span><span class="p">:</span> <span class="nb">Option</span><span class="o">&lt;</span><span class="nb">i32</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nf">Some</span><span class="p">(</span><span class="mi">42</span><span class="p">);</span>
<span class="k">let</span> <span class="n">result</span><span class="p">:</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="nb">i32</span><span class="p">,</span> <span class="o">&amp;</span><span class="nb">str</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nf">Ok</span><span class="p">(</span><span class="mi">42</span><span class="p">);</span>
<span class="c1">// Unwrap value, if None returns the default the type</span>
<span class="k">let</span> <span class="n">value</span> <span class="o">=</span> <span class="n">maybe_value</span><span class="nf">.unwrap_or_default</span><span class="p">();</span>
<span class="c1">// Multiply a valid result by 2</span>
<span class="k">let</span> <span class="n">result2</span> <span class="o">=</span> <span class="n">result</span><span class="nf">.map</span><span class="p">(|</span><span class="n">x</span><span class="p">|</span> <span class="n">x</span> <span class="o">*</span> <span class="mi">2</span><span class="p">);</span>
</code></pre></div></div> <p>In Rust, it is recommend to use pattern matching to handle these types appropriately.</p> <h1 id="parsing-integers-from-stdin">Parsing Integers from Stdin</h1> <h2 id="reading-input">Reading Input</h2> <p>Use <code class="language-plaintext highlighter-rouge">std::io::stdin()</code> to read input from the standard input:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="k">mut</span> <span class="n">input</span> <span class="o">=</span> <span class="nn">String</span><span class="p">::</span><span class="nf">new</span><span class="p">();</span>
<span class="nn">std</span><span class="p">::</span><span class="nn">io</span><span class="p">::</span><span class="nf">stdin</span><span class="p">()</span><span class="nf">.read_line</span><span class="p">(</span><span class="o">&amp;</span><span class="k">mut</span> <span class="n">input</span><span class="p">)</span>
    <span class="nf">.expect</span><span class="p">(</span><span class="s">"Failed to read line"</span><span class="p">);</span>
</code></pre></div></div> <h1 id="parsing-integer-from-stdin">Parsing Integer from Stdin</h1> <h2 id="parsing-integers">Parsing Integers</h2> <p>Convert the input to a string using <code class="language-plaintext highlighter-rouge">parse</code>:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">let</span> <span class="n">parsed_num</span><span class="p">:</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="nb">i32</span><span class="p">,</span> <span class="n">_</span><span class="o">&gt;</span> <span class="o">=</span> <span class="n">input</span><span class="nf">.trim</span><span class="p">()</span><span class="nf">.parse</span><span class="p">();</span>
<span class="k">match</span> <span class="n">parsed_num</span> <span class="p">{</span>
    <span class="nf">Ok</span><span class="p">(</span><span class="n">number</span><span class="p">)</span> <span class="k">=&gt;</span> <span class="c1">// Use the parsed integer,</span>
    <span class="nf">Err</span><span class="p">(</span><span class="n">_</span><span class="p">)</span> <span class="k">=&gt;</span> <span class="c1">// Handle parsing error,</span>
<span class="p">}</span>
</code></pre></div></div> <h1 id="reliably-parsing-various-integers">Reliably Parsing Various Integers</h1> <h2 id="step-1">Step 1</h2> <p>Create a helper function to parse integers from a string:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">#[derive(Debug)]</span>
<span class="k">struct</span> <span class="nf">ParserError</span><span class="p">(</span><span class="nb">String</span><span class="p">);</span>

<span class="k">fn</span> <span class="nf">usize</span><span class="p">(</span><span class="nb">str</span><span class="p">:</span> <span class="o">&amp;</span><span class="nb">str</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="nb">usize</span><span class="p">,</span> <span class="n">ParserError</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="nb">str</span><span class="nf">.trim</span><span class="p">()</span>
        <span class="py">.parse</span><span class="p">::</span><span class="o">&lt;</span><span class="nb">usize</span><span class="o">&gt;</span><span class="p">()</span>
        <span class="nf">.map_err</span><span class="p">(|</span><span class="n">e</span><span class="p">|</span> <span class="nf">ParserError</span><span class="p">(</span><span class="nd">format!</span><span class="p">(</span><span class="s">"{}"</span><span class="p">,</span> <span class="n">e</span><span class="p">)))</span>
<span class="p">}</span>
</code></pre></div></div> <p>Note that, we don’t handle the error directly, we simply propagate it to the client using a special error <code class="language-plaintext highlighter-rouge">ParserError</code>.</p> <h1 id="reliably-parsing-various-integers-1">Reliably Parsing Various Integers</h1> <h2 id="step-2">Step 2</h2> <p>With a line from the stdin parse as many integers as you would like. Here, we assume integers are separated by a white space.</p> <p>\scriptsize</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="nf">pair</span><span class="p">()</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="p">(</span><span class="nb">usize</span><span class="p">,</span> <span class="nb">usize</span><span class="p">),</span> <span class="n">ParserError</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="k">let</span> <span class="k">mut</span> <span class="n">buffer</span> <span class="o">=</span> <span class="nn">String</span><span class="p">::</span><span class="nf">new</span><span class="p">();</span>
    <span class="nn">std</span><span class="p">::</span><span class="nn">io</span><span class="p">::</span><span class="nf">stdin</span><span class="p">()</span>
        <span class="nf">.read_line</span><span class="p">(</span><span class="o">&amp;</span><span class="k">mut</span> <span class="n">buffer</span><span class="p">)</span>
        <span class="nf">.map_err</span><span class="p">(|</span><span class="n">e</span><span class="p">|</span> <span class="nf">ParserError</span><span class="p">(</span><span class="nd">format!</span><span class="p">(</span><span class="s">"{}"</span><span class="p">,</span> <span class="n">e</span><span class="p">)))</span><span class="o">?</span><span class="p">;</span>
    <span class="k">let</span> <span class="k">mut</span> <span class="n">nums</span> <span class="o">=</span> <span class="n">buffer</span><span class="nf">.split</span><span class="p">(</span><span class="s">" "</span><span class="p">);</span>
    <span class="k">let</span> <span class="n">fst</span> <span class="o">=</span> <span class="n">nums</span>
        <span class="nf">.next</span><span class="p">()</span>
        <span class="nf">.ok_or_else</span><span class="p">(||</span> <span class="nf">ParserError</span><span class="p">(</span><span class="nd">format!</span><span class="p">(</span><span class="s">"fst"</span><span class="p">)))</span>
        <span class="nf">.and_then</span><span class="p">(|</span><span class="n">s</span><span class="p">|</span> <span class="nf">usize</span><span class="p">(</span><span class="n">s</span><span class="p">))</span><span class="o">?</span><span class="p">;</span>
    <span class="k">let</span> <span class="n">snd</span> <span class="o">=</span> <span class="c1">// same as above ...</span>
    <span class="nf">Ok</span><span class="p">((</span><span class="n">fst</span><span class="p">,</span> <span class="n">snd</span><span class="p">))</span>
<span class="p">}</span>
</code></pre></div></div> <h1 id="error-propagation">Error Propagation</h1> <h2 id="try-operator">Try Operator</h2> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">std</span><span class="p">::</span><span class="nn">io</span><span class="p">::</span><span class="nf">stdin</span><span class="p">()</span>
    <span class="nf">.read_line</span><span class="p">(</span><span class="o">&amp;</span><span class="k">mut</span> <span class="n">buffer</span><span class="p">)</span>
    <span class="nf">.map_err</span><span class="p">(|</span><span class="n">e</span><span class="p">|</span> <span class="nf">ParserError</span><span class="p">(</span><span class="nd">format!</span><span class="p">(</span><span class="s">"{}"</span><span class="p">,</span> <span class="n">e</span><span class="p">)))</span><span class="o">?</span><span class="p">;</span> <span class="c1">// &lt;-</span>
</code></pre></div></div> <p>Note the special use of the <code class="language-plaintext highlighter-rouge">?</code> operator in the previous code. The <code class="language-plaintext highlighter-rouge">?</code> operator in Rust streamlines error propagation. Used in functions returning <code class="language-plaintext highlighter-rouge">Result</code>, it unwraps the value on <code class="language-plaintext highlighter-rouge">Ok</code> or short-circuits to return <code class="language-plaintext highlighter-rouge">ParserError</code> early. This simplifies error handling, reducing the need for explicit <code class="language-plaintext highlighter-rouge">match</code> or <code class="language-plaintext highlighter-rouge">unwrap</code> statements and contributing to concise, readable code.</p> <h1 id="ownership-in-rust">Ownership in Rust</h1> <p>In Rust, each value has a variable that is its “owner”:</p> <ul> <li>Ownership of a value can be transferred from one variable to another</li> <li>The original variable can no longer be used</li> </ul> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="nf">transfer_ownership</span><span class="p">(</span><span class="n">s</span><span class="p">:</span> <span class="nb">String</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// s is the owner of a String</span>
    <span class="c1">// Ownership transferred to this function</span>
<span class="p">}</span> <span class="c1">// s goes out of scope, memory freed</span>
<span class="k">let</span> <span class="n">x</span> <span class="o">=</span> <span class="nn">String</span><span class="p">::</span><span class="nf">from</span><span class="p">(</span><span class="s">"Hello"</span><span class="p">);</span>
<span class="c1">// Ownership transferred to the function</span>
<span class="nf">transfer_ownership</span><span class="p">(</span><span class="n">x</span><span class="p">);</span>
<span class="c1">// println!("{}", x); // Error: x is no longer valid</span>
</code></pre></div></div> <h1 id="borrowing-in-rust--immutable-borrowing">Borrowing in Rust – Immutable Borrowing</h1> <p>Borrowing allows references to values without transferring ownership:</p> <ul> <li>References can be mutable or immutable</li> <li>Multiple references can borrow a value immutably: <ul> <li>No changes can be made</li> </ul> </li> </ul> <p>Example:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="nf">immutable_borrow</span><span class="p">(</span><span class="n">s</span><span class="p">:</span> <span class="o">&amp;</span><span class="nb">String</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// s is an immutable reference to a String</span>
<span class="p">}</span>
<span class="k">let</span> <span class="n">x</span> <span class="o">=</span> <span class="nn">String</span><span class="p">::</span><span class="nf">from</span><span class="p">(</span><span class="s">"Hello"</span><span class="p">);</span>
<span class="nf">immutable_borrow</span><span class="p">(</span><span class="o">&amp;</span><span class="n">x</span><span class="p">);</span> <span class="c1">// Passing an immutable reference</span>
</code></pre></div></div> <h1 id="borrowing-in-rust--mutable-borrowing">Borrowing in Rust – Mutable Borrowing</h1> <p>Only one mutable reference is allowed at a time:</p> <ul> <li>Prevents data races</li> </ul> <p>Example:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="nf">mutable_borrow</span><span class="p">(</span><span class="n">s</span><span class="p">:</span> <span class="o">&amp;</span><span class="k">mut</span> <span class="nb">String</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// s is a mutable reference to a String</span>
<span class="p">}</span>
<span class="k">let</span> <span class="k">mut</span> <span class="n">x</span> <span class="o">=</span> <span class="nn">String</span><span class="p">::</span><span class="nf">from</span><span class="p">(</span><span class="s">"Hello"</span><span class="p">);</span>
<span class="nf">mutable_borrow</span><span class="p">(</span><span class="o">&amp;</span><span class="k">mut</span> <span class="n">x</span><span class="p">);</span> <span class="c1">// Passing a mutable reference</span>
</code></pre></div></div> <h1 id="heap-allocation-in-rust">Heap Allocation in Rust</h1> <p>Rust allows dynamic allocation on the Heap. Ownership ensures memory safety without GC.</p> <ul> <li>The <code class="language-plaintext highlighter-rouge">Box</code> type is used for heap allocating</li> <li>Boxes have a single owner</li> </ul> <p>\small</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="nf">heap_allocation</span><span class="p">()</span> <span class="k">-&gt;</span> <span class="nb">Box</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">i32</span><span class="o">&gt;&gt;</span> <span class="p">{</span>
    <span class="c1">// Allocating a Vec&lt;i32&gt; on the heap and returning a Box</span>
    <span class="nn">Box</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="nd">vec!</span><span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">])</span>
<span class="p">}</span>
<span class="c1">// Ownership transferred to the variable data</span>
<span class="k">let</span> <span class="n">data</span> <span class="o">=</span> <span class="nf">heap_allocation</span><span class="p">();</span>
</code></pre></div></div> <h1 id="heap-allocation-in-rust--dropping-and-cleaning">Heap Allocation in Rust – Dropping and Cleaning</h1> <p>Ownership rules govern cleanup; no need for explicit deallocation:</p> <ul> <li>When a variable goes out of scope, its memory is automatically freed</li> </ul> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
    <span class="k">let</span> <span class="n">data</span> <span class="o">=</span> <span class="nn">Box</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="nn">String</span><span class="p">::</span><span class="nf">from</span><span class="p">(</span><span class="s">"Heap Allocated"</span><span class="p">));</span>
<span class="p">}</span> <span class="c1">// Memory is freed here</span>
</code></pre></div></div> <h1 id="practical-example--parsing-and-storing-a-graph-in-the-heap">Practical Example – Parsing and Storing a Graph in the Heap</h1> <h2 id="step-1---choose-a-memory-layout">Step 1 - Choose a Memory Layout</h2> <p>To first parse a directed graph, $G$, choose how you’re going to represent it on memory, for example, using a adjacency list:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="n">Graph</span> <span class="p">{</span>
    <span class="n">v</span><span class="p">:</span> <span class="nb">usize</span><span class="p">,</span>
    <span class="n">adj</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">usize</span><span class="o">&gt;&gt;</span><span class="p">,</span>
    <span class="n">rev_adj</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">usize</span><span class="o">&gt;&gt;</span><span class="p">,</span>
<span class="p">}</span>
</code></pre></div></div> <p>In this <code class="language-plaintext highlighter-rouge">struct</code> we store:</p> <ul> <li><code class="language-plaintext highlighter-rouge">v</code>, the number of vertexes</li> <li><code class="language-plaintext highlighter-rouge">adj</code>, the adjacency list of the directed graph $G$</li> <li><code class="language-plaintext highlighter-rouge">rev_adj</code>, the reverse adjacency list of the graph, denoted $G^T$</li> </ul> <h1 id="practical-example--parsing-and-storing-a-graph-in-the-heap-1">Practical Example – Parsing and Storing a Graph in the Heap</h1> <h2 id="step-2---parse-the-graph-from-the-stdin">Step 2 - Parse the Graph from the Stdin</h2> <ul> <li>Read the edges from stdin and store them in the adjacency list</li> <li>Return a reference to a heap allocated graph</li> </ul> <p>\scriptsize</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="nf">parse_graph</span><span class="p">()</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="nb">Box</span><span class="o">&lt;</span><span class="n">Graph</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">ParserError</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="k">let</span> <span class="p">(</span><span class="n">v</span><span class="p">,</span> <span class="k">mut</span> <span class="n">e</span><span class="p">)</span> <span class="o">=</span> <span class="nf">pair</span><span class="p">()</span><span class="o">?</span><span class="p">;</span>
    <span class="k">let</span> <span class="k">mut</span> <span class="n">adj</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">usize</span><span class="o">&gt;&gt;</span> <span class="o">=</span> <span class="nd">vec!</span><span class="p">[</span><span class="nd">vec!</span><span class="p">[];</span> <span class="n">v</span><span class="p">];</span>
    <span class="k">let</span> <span class="k">mut</span> <span class="n">rev_adj</span><span class="p">:</span> <span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">usize</span><span class="o">&gt;&gt;</span> <span class="o">=</span> <span class="nd">vec!</span><span class="p">[</span><span class="nd">vec!</span><span class="p">[];</span> <span class="n">v</span><span class="p">];</span>
    <span class="k">while</span> <span class="n">e</span> <span class="o">&gt;</span> <span class="mi">0</span> <span class="p">{</span>
        <span class="k">let</span> <span class="p">(</span><span class="n">u</span><span class="p">,</span> <span class="n">w</span><span class="p">)</span> <span class="o">=</span> <span class="nf">pair</span><span class="p">()</span><span class="o">?</span><span class="p">;</span>
        <span class="nd">assert!</span><span class="p">((</span><span class="n">u</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">v</span><span class="p">);</span>
        <span class="nd">assert!</span><span class="p">((</span><span class="n">w</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">v</span><span class="p">);</span>
        <span class="n">adj</span><span class="p">[</span><span class="n">u</span> <span class="o">-</span> <span class="mi">1</span><span class="p">]</span><span class="nf">.push</span><span class="p">(</span><span class="n">w</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
        <span class="n">rev_adj</span><span class="p">[</span><span class="n">w</span> <span class="o">-</span> <span class="mi">1</span><span class="p">]</span><span class="nf">.push</span><span class="p">(</span><span class="n">u</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
        <span class="n">e</span> <span class="o">=</span> <span class="n">e</span> <span class="o">-</span> <span class="mi">1</span>
    <span class="p">}</span>
    <span class="nf">Ok</span><span class="p">(</span><span class="nn">Box</span><span class="p">::</span><span class="nf">new</span><span class="p">(</span><span class="n">Graph</span> <span class="p">{</span> <span class="n">v</span><span class="p">,</span> <span class="n">adj</span><span class="p">,</span> <span class="n">rev_adj</span> <span class="p">}))</span>
<span class="p">}</span>
</code></pre></div></div> <h1 id="practical-example--parsing-and-storing-a-graph-in-the-heap-2">Practical Example – Parsing and Storing a Graph in the Heap</h1> <h2 id="step-3---entry-point">Step 3 - Entry point</h2> <p>The lifetime of the graph is only within its owner scope:</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="nf">main</span><span class="p">()</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="p">(),</span> <span class="n">ParserError</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="c1">// Parse the graph and obtain ownership</span>
    <span class="k">let</span> <span class="n">graph</span> <span class="o">=</span> <span class="nf">parse_graph</span><span class="p">()</span><span class="o">?</span><span class="p">;</span>

    <span class="c1">// Perform operations with the graph</span>
    <span class="c1">// ...</span>

    <span class="c1">// Graph ownership goes out of scope, and</span>
    <span class="c1">// memory is automatically freed</span>
    <span class="nf">Ok</span><span class="p">(())</span>
<span class="p">}</span>
</code></pre></div></div> <h1 id="tips-rusts-smart-referencing">Tips: Rust’s Smart Referencing</h1> <p>Rust’s implicit reference coercion automatically converts certain types to references when needed, simplifying the use of owned values in contexts requiring references and eliminating explicit conversion syntax.</p> <p>\scriptsize</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="nf">process_graph</span><span class="p">(</span><span class="n">g</span><span class="p">:</span> <span class="o">&amp;</span><span class="n">Graph</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Automatically coerces Box&lt;Graph&gt; to &amp;Graph when passed</span>
<span class="p">}</span>
<span class="k">fn</span> <span class="nf">main</span><span class="p">()</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="p">(),</span> <span class="n">ParserError</span><span class="o">&gt;</span> <span class="p">{</span>
    <span class="c1">// Parse the graph and box it on the heap</span>
    <span class="k">let</span> <span class="n">boxed_graph</span><span class="p">:</span> <span class="nb">Box</span><span class="o">&lt;</span><span class="n">Graph</span><span class="o">&gt;</span> <span class="o">=</span> <span class="nf">parse_graph</span><span class="p">()</span><span class="o">?</span><span class="p">;</span>
    <span class="c1">// Pass the boxed graph to a function expecting a reference</span>
    <span class="nf">process_graph</span><span class="p">(</span><span class="o">&amp;</span><span class="n">boxed_graph</span><span class="p">);</span> <span class="c1">// Implicit reference coercion</span>
    <span class="nf">Ok</span><span class="p">(())</span>
<span class="p">}</span>
</code></pre></div></div> <h1 id="tips-rusts-smart-dereference">Tips: Rust’s Smart Dereference</h1> <p>Rust’s smart dereference coercion simplifies the process of dereferencing, automatically converting certain types to references when needed. This eliminates the need for explicit dereferencing syntax.</p> <p>\small</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="nf">process_graph</span><span class="p">(</span><span class="n">g</span><span class="p">:</span> <span class="o">&amp;</span><span class="n">Graph</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// Automatically coerces &amp;Graph to Graph when</span>
    <span class="c1">// dereferencing parameter v</span>
    <span class="nd">println!</span><span class="p">(</span><span class="s">"Number of vertices: {}"</span><span class="p">,</span> <span class="n">g</span><span class="py">.v</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <h1 id="tips-efficient-code-with-asserts">Tips: Efficient Code with Asserts</h1> <p>Leveraging asserts at the top of a function can optimize code execution by enabling the Rust compiler to eliminate runtime bounds checks when iterating through a vector <code class="language-plaintext highlighter-rouge">data</code> using indices.</p> <p>\small</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="nf">process</span><span class="p">(</span><span class="n">data</span><span class="p">:</span> <span class="o">&amp;</span><span class="nb">Vec</span><span class="o">&lt;</span><span class="nb">i32</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">size</span><span class="p">:</span> <span class="nb">usize</span><span class="p">)</span> <span class="p">{</span>
    <span class="nd">assert!</span><span class="p">(</span><span class="n">data</span><span class="nf">.len</span><span class="p">()</span> <span class="o">&gt;=</span> <span class="n">size</span><span class="p">);</span>

    <span class="k">for</span> <span class="n">i</span> <span class="k">in</span> <span class="mi">0</span><span class="o">..</span><span class="n">size</span> <span class="p">{</span>
        <span class="c1">// Internal vector bounds checks can be</span>
        <span class="c1">// optimized away because of the assert invariant</span>
        <span class="n">data</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">...</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <h1 id="tips-useful-resources">Tips: Useful resources</h1> <p>Rust’s standard library: <a href="https://doc.rust-lang.org/std/">https://doc.rust-lang.org/std/</a></p> <ul> <li>Indispensable</li> </ul> <p>Rust by example: <a href="https://doc.rust-lang.org/rust-by-example/">https://doc.rust-lang.org/rust-by-example/</a></p> <ul> <li>Examples that illustrate various Rust concepts</li> </ul> <p>The Rust Programming Language: <a href="https://doc.rust-lang.org/book/">https://doc.rust-lang.org/book/</a></p>]]></content><author><name></name></author><category term="code"/><category term="tutorial"/><summary type="html"><![CDATA[Prelude]]></summary></entry><entry><title type="html">Static Analysis Lab: C</title><link href="https://www.filipeom.dev/blog/2023/static-analysis-lab/" rel="alternate" type="text/html" title="Static Analysis Lab: C"/><published>2023-10-02T00:00:00+00:00</published><updated>2023-10-02T00:00:00+00:00</updated><id>https://www.filipeom.dev/blog/2023/static-analysis-lab</id><content type="html" xml:base="https://www.filipeom.dev/blog/2023/static-analysis-lab/"><![CDATA[<h1 id="prelude">Prelude</h1> <p>This script was made for the 2023/2024 Software Specification course at Instituto Superior Técnico.</p> <h1 id="introduction">Introduction</h1> <p>Before we dive into the lab exercises, let’s briefly discuss some essential concepts.</p> <h2 id="background">Background</h2> <p><strong>Nullptr Deference.</strong> In C and C++ programming, a nullptr dereference occurs when a program attempts to access or manipulate a memory location using a null pointer (a pointer that doesn’t point to any valid memory address). This often leads to runtime errors, crashes, or unexpected behavior in your programs. Here’s an example:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
	<span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
	<span class="o">*</span><span class="n">ptr</span> <span class="o">=</span> <span class="mi">42</span><span class="p">;</span> <span class="c1">// This is a null pointer dereference</span>
	<span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>In the above code, we have a pointer <code class="language-plaintext highlighter-rouge">ptr</code> initialized to <code class="language-plaintext highlighter-rouge">NULL</code>. The line <code class="language-plaintext highlighter-rouge">*ptr = 42;</code> attempts to assign a value to the memory location pointed to by <code class="language-plaintext highlighter-rouge">ptr</code>, but since <code class="language-plaintext highlighter-rouge">ptr</code> is null, this results in a null pointer dereference.</p> <p><strong>Buffer Overflow.</strong> A buffer overflow occurs when a program writes more data to a buffer (e.g., an array) than it can hold, leading to data corruption and potentially security vulnerabilities. Here’s an example:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kt">char</span> <span class="n">buffer</span><span class="p">[</span><span class="mi">5</span><span class="p">];</span>
    <span class="n">strcpy</span><span class="p">(</span><span class="n">buffer</span><span class="p">,</span> <span class="s">"Hello, World!"</span><span class="p">);</span>  <span class="c1">// Buffer overflow</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"%s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">buffer</span><span class="p">);</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>In this code, the <code class="language-plaintext highlighter-rouge">strcpy</code> function writes more characters to the <code class="language-plaintext highlighter-rouge">buffer</code> than it can accommodate, resulting in a buffer overflow.</p> <p><strong>Use-After-Free.</strong> Use-after-free errors occur when a program attempts to access memory that has been deallocated or freed. These errors can lead to undefined behavior. For example:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="p">(</span><span class="kt">int</span><span class="o">*</span><span class="p">)</span><span class="n">malloc</span><span class="p">(</span><span class="k">sizeof</span><span class="p">(</span><span class="kt">int</span><span class="p">));</span>
    <span class="n">free</span><span class="p">(</span><span class="n">ptr</span><span class="p">);</span>
    <span class="o">*</span><span class="n">ptr</span> <span class="o">=</span> <span class="mi">42</span><span class="p">;</span>  <span class="c1">// Use-after-free</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>In this code, the memory pointed to by <code class="language-plaintext highlighter-rouge">ptr</code> is freed before it is accessed, causing a use-after-free error.</p> <p><strong>Memory Leak.</strong> A memory leak happens when a program allocates memory but fails to deallocate it, causing memory to be unavailable for the rest of the program’s execution. For example:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kt">int</span><span class="o">*</span> <span class="n">ptr</span> <span class="o">=</span> <span class="p">(</span><span class="kt">int</span><span class="o">*</span><span class="p">)</span><span class="n">malloc</span><span class="p">(</span><span class="k">sizeof</span><span class="p">(</span><span class="kt">int</span><span class="p">));</span>
    <span class="c1">// Memory is not freed</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>In this code, memory is allocated but not freed, resulting in a memory leak.</p> <p><strong>Uninitialized Variables.</strong> Uninitialized variables are variables that are used before being assigned a value. This can lead to undefined behavior and unexpected results. Infer can identify uninitialized variable issues. For example:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
<span class="kt">int</span> <span class="n">x</span><span class="p">;</span>
<span class="n">printf</span><span class="p">(</span><span class="s">"%d</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">x</span><span class="p">);</span> <span class="c1">// Uninitialized variable</span>
<span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>In this code, the variable <code class="language-plaintext highlighter-rouge">x</code> is used in the <code class="language-plaintext highlighter-rouge">printf</code> statement before being initialized, resulting in an uninitialized variable issue.</p> <p><strong>Static Analysis.</strong> Static analysis is a program analysis technique that examines the source code without actually executing it. It aims to identify potential issues, bugs, and vulnerabilities in the code before runtime. Static analysis tools can quickly provide valuable insights into code quality and safety. In this lab we will delve into the realm of static analysis, leveraging two cutting-edge tools designed for analysing C code:</p> <ul> <li><strong>clang-analyzer:</strong> The <a href="https://clang-analyzer.llvm.org/scan-build.html">clang-analyzer</a> is a part of the Clang compiler suite that provides static analysis capabilities. It is designed to detect various types of programming errors, including null-pointer dereferences, resource leaks, and more.</li> <li><strong>Infer:</strong> <a href="https://fbinfer.com/">Infer</a> is another powerful static analysis tool, similar to the <em>clang-analyzer</em>, that we will use in this lab to automatically detect programming errors.</li> </ul> <p>Now that you have some context, let’s proceed with setting up clang-analyzer and Infer and working on the lab exercises.</p> <h2 id="getting-started">Getting Started</h2> <p>To begin, please download and extract the file provided on the class page (or <a href="https://filipeom.github.io/assets/zip/lab-static-analysis.zip">here</a>):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>unzip lab-static-analysis.zip
</code></pre></div></div> <p>It’s important to note that both clang-analyzer and Infer are currently only available on Unix-like operating systems, such as Linux and macOS. Therefore, if you are using Windows, we highly recommend using <a href="https://learn.microsoft.com/en-us/windows/wsl/install">WSL 2 (Windows Subsystem for Linux)</a>. Below, we provide step-by-step instructions on how to install clang-analyzer and Infer. Notably, for this lab, using Infer’s Docker image is highly recommended as it comes pre-installed with both Infer and clang-analyzer (instructions on Docker here: <a href="#docker">Getting Infer: Docker Image</a>).</p> <h2 id="getting-clang-analyzer">Getting Clang-Analyzer</h2> <p>To install clang-analyzer, you can follow these steps based on your operating system below.</p> <p>It’s worth noting that if you opt for the recommended Infer Docker image, clang-analyzer comes pre-installed. You can find instructions for setting up the Infer Docker image here: <a href="#docker">Getting Infer: Docker Image</a>.</p> <h3 id="macos">macOS</h3> <p>Simply use Homebrew:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>brew <span class="nb">install </span>llvm
</code></pre></div></div> <h3 id="ubuntu-and-ubuntu-like-linux-distributions">Ubuntu and Ubuntu-like Linux Distributions</h3> <p>On Ubuntu and similar Linux distributions, you can use the following command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">sudo </span>apt-get <span class="nb">install </span>clang clang-tools perl
</code></pre></div></div> <h2 id="getting-infer-binary-releases">Getting Infer: Binary Releases</h2> <p>On macOS, the simplest way to install Infer is by using Homebrew:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>brew <span class="nb">install </span>infer
</code></pre></div></div> <p>On Linux, or if you prefer not to use Homebrew, you can download the <a href="https://github.com/facebook/infer/releases/">latest release from GitHub</a> and install it locally (recommended):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cd </span>lab-sa/infer
<span class="nv">$ </span>curl <span class="nt">-SLO</span> <span class="s2">"https://github.com/facebook/infer/releases/download/</span><span class="se">\</span><span class="s2">
v1.1.0/infer-linux64-v1.1.0.tar.xz"</span>
<span class="nv">$ </span><span class="nb">tar</span> <span class="nt">-xvf</span> infer-linux64-v1.1.0.tar.xz <span class="o">&amp;&amp;</span> <span class="se">\</span>
<span class="nb">ln</span> <span class="nt">-s</span> <span class="s2">"</span><span class="nv">$PWD</span><span class="s2">/infer-linux64-v1.1.0/bin/infer"</span> ~/.local/bin/infer
</code></pre></div></div> <p>Alternatively, if you wish to install Infer system-wide (install at your own risk):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cd </span>lab-sa/infer
<span class="nv">$ </span>curl <span class="nt">-SLO</span> <span class="s2">"https://github.com/facebook/infer/releases/download/</span><span class="se">\</span><span class="s2">
v1.1.0/infer-linux64-v1.1.0.tar.xz"</span>
<span class="nv">$ </span><span class="nb">sudo tar</span> <span class="nt">-C</span> /opt <span class="nt">-xvf</span> infer-linux64-v1.1.0.tar.xz <span class="o">&amp;&amp;</span> <span class="se">\</span>
<span class="nb">sudo ln</span> <span class="nt">-s</span> /opt/infer-linux64-v1.1.0/bin/infer /usr/local/bin/infer
</code></pre></div></div> <h2 id="docker">Getting Infer: Docker Image</h2> <p>You also have the option to use Infer’s Docker image, conveniently included in the lab zipfile. However, if you plan to run Docker inside the lab computers, please be aware that you’ll need to execute a few configuration commands before building Infer’s Docker image. Comprehensive instructions for this can be found in our FAQ\footnote{\url{https://rnl.tecnico.ulisboa.pt/faq/#docker}} (reproduced below for your convenience). If you are not utilizing lab computers, you may skip the subsequent paragraph (i.e., <strong>Setup Docker</strong>).</p> <p><strong>Setup Docker.</strong> The following command should only be executed <em>once</em>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>setup-docker
</code></pre></div></div> <p>Then, add the following lines to your <code class="language-plaintext highlighter-rouge">~/.bashrc</code> file (changes take place after opening a new terminal).</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">PATH</span><span class="o">=</span><span class="nv">$HOME</span>/bin:<span class="nv">$PATH</span>
<span class="nb">export </span><span class="nv">DOCKER_HOST</span><span class="o">=</span>unix://<span class="nv">$XDG_RUNTIME_DIR</span>/docker.sock
</code></pre></div></div> <p>Lastly, after every login users must run the following command to use docker:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>systemctl <span class="nt">--user</span> start docker
</code></pre></div></div> <p><strong>Build &amp; Run Infer.</strong> Ensure that docker is running as explained in the paragraph above and then execute:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cd </span>lab-sa/infer/docker
<span class="nv">$ </span>docker build <span class="nt">-t</span> infer <span class="nb">.</span>
<span class="c"># mount the local lab directory inside the image</span>
<span class="nv">$ </span>docker run <span class="nt">--net</span><span class="o">=</span>host <span class="nt">-it</span> <span class="nt">-v</span> <span class="nv">$PWD</span>/../../:/lab-sa infer /bin/bash
<span class="c"># you should now be inside the docker container with a shell prompt,</span>
<span class="c"># e.g. "root@hostname:/#"</span>
<span class="nv">$ </span><span class="nb">cd </span>lab-sa/exercise01/
<span class="nv">$ </span>infer <span class="nt">--</span> clang <span class="nt">-c</span> hello.c
</code></pre></div></div> <h1 id="exercise-01-hello-world">Exercise 01: Hello, World!</h1> <p>This exercise revolves around the program <code class="language-plaintext highlighter-rouge">hello.c</code>, which is provided alongside the project script in the <code class="language-plaintext highlighter-rouge">lab-sa/exercise01</code> directory.</p> <h3 id="infer">Infer</h3> <p>To automatically detect bugs in the program, utilize Infer by executing the following command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cd </span>lab-sa/exercise01
<span class="nv">$ </span>infer run <span class="nt">--</span> clang <span class="nt">-c</span> hello.c
</code></pre></div></div> <p>Once the Infer run is successful, you can delve into more comprehensive reports generated by Infer by running <code class="language-plaintext highlighter-rouge">infer explore</code> from the same directory.</p> <h3 id="clang-analyzer">Clang-Analyzer</h3> <p>To automatically identify and analyze potential bugs in your program, you can utilize <code class="language-plaintext highlighter-rouge">scan-build</code>. Follow these steps to execute the analysis:</p> <p>First, run the following command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>scan-build clang <span class="nt">-c</span> hello.c
</code></pre></div></div> <p>Upon running the command, you should see an output similar to the following:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hello.c:5:8: warning: Dereference of null pointer <span class="o">(</span>loaded from variable
	<span class="s1">'ptr'</span><span class="o">)</span> <span class="o">[</span>core.NullDereference]
  <span class="k">*</span>ptr <span class="o">=</span> 42<span class="p">;</span>
   ~~~ ^
1 warning generated.
scan-build: Analysis run complete.
scan-build: 1 bug found.
scan-build: Run <span class="s1">'scan-view /tmp/scan-build-2023-10-01-094934-161859-1'</span>
	to examine bug reports.
</code></pre></div></div> <p>The output provides an initial summary of detected issues. To gain a more comprehensive understanding of these issues, you can examine the bug reports using <code class="language-plaintext highlighter-rouge">scan-view</code>. Execute the following command, replacing the path with the appropriate directory:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>scan-view /tmp/scan-build-2023-10-01-094934-161859-1
<span class="c"># Which produces a webserver at 127.0.0.1:8181 to examine bug reports</span>
Starting scan-view at: http://127.0.0.1:8181
  Use Ctrl-C to exit.
</code></pre></div></div> <p>Running the command will start a web server at <code class="language-plaintext highlighter-rouge">http://127.0.0.1:8181</code> where you can examine the bug reports in a user-friendly interface. To access the reports, open a web browser and navigate to the provided URL.</p> <h1 id="exercise-02-infer-workflow">Exercise 02: Infer Workflow</h1> <p>In an Infer run, there are two key phases:</p> <ul> <li><strong>Capture Phase:</strong> During this phase, Infer captures compilation commands to translate the files for analysis into Infer’s internal intermediate language.</li> <li><strong>Analysis Phase:</strong> In this phase, Infer conducts a separate analysis of each function and method. If Infer encounters an error while analyzing a method or function, it stops the analysis for that specific entity but continues to analyze others.</li> </ul> <p>This phased analysis is especially useful when continuously analysing big projects with multiple files.</p> <p>In this exercise, you’ll work with two programs: <code class="language-plaintext highlighter-rouge">proj01.c</code> and <code class="language-plaintext highlighter-rouge">proj02.c</code>, which are provided in the <code class="language-plaintext highlighter-rouge">lab-sa/exercise02</code> directory. The exercise encompasses four main objectives:</p> <ol> <li>Analyse the <code class="language-plaintext highlighter-rouge">proj01.c</code> program using Infer and clang-analyzer.</li> <li>Manually identify all bugs in the program and report them in a YAML file.</li> <li>Correct the bugs and use Infer or clang-analyzer to verify that there are no remaining warnings.</li> <li>Repeat the entire process, including steps 1 to 3, for the <code class="language-plaintext highlighter-rouge">proj02.c</code> program.</li> </ol> <p>To accomplish these goals, follow these steps:</p> <h2 id="infer-1">Infer</h2> <h3 id="step-1-run-infer">Step 1: Run Infer</h3> <p>Capture the C project using:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>infer capture <span class="nt">--</span> clang <span class="nt">-c</span> proj01.c
</code></pre></div></div> <p>Analyse the project with Infer:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>infer analyze
</code></pre></div></div> <p>Alternatively, to run both phases in a single command, use:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>infer run <span class="nt">--</span> clang <span class="nt">-c</span> proj01.c
</code></pre></div></div> <h3 id="step-2-identify-and-report-bugs">Step 2: Identify and Report Bugs</h3> <p>Examine the report generated by Infer more closely with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>infer explore
</code></pre></div></div> <p>Create a YAML file (e.g., <code class="language-plaintext highlighter-rouge">bug_report.yml</code>) based on the <code class="language-plaintext highlighter-rouge">report.yml</code> template provided in the directory to report the identified bugs. For example:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">report</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">bug</span><span class="pi">:</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">Nullptr Dereference</span>
      <span class="na">lineno</span><span class="pi">:</span> <span class="m">42</span>
      <span class="na">class</span><span class="pi">:</span> <span class="s">tp</span>

  <span class="pi">-</span> <span class="na">bug</span><span class="pi">:</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">Example Bug Type</span>
      <span class="na">lineno</span><span class="pi">:</span> <span class="m">67</span>
      <span class="na">class</span><span class="pi">:</span> <span class="s">fn</span>
</code></pre></div></div> <p>In the YAML file, specify the bug type, line number, and class (refer to <code class="language-plaintext highlighter-rouge">report.yml</code> for available class options). This structured report will help in documenting and addressing the identified issues effectively (and will also be in the class project).</p> <h3 id="step-3-bug-resolution">Step 3: Bug Resolution</h3> <ul> <li>Address all the identified bugs.</li> <li>Create a new version of the file (e.g., <code class="language-plaintext highlighter-rouge">proj01-fixed.c</code>), ensuring that all discovered bugs are fixed, and Infer no longer produces any warnings.</li> </ul> <h2 id="clang-analyzer-1">Clang-analyzer</h2> <p>One benefit of the clang-analyzer is that is comes with many default analysis checkers, but also optin performance and bug checkers, for more information refer to <code class="language-plaintext highlighter-rouge">scan-build --help</code>. In this exercise we will play with different checkers to discover more bugs.</p> <h3 id="step-1-initial-analysis">Step 1: Initial Analysis</h3> <p>Begin by analyzing the project using the default checkers with the following command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>scan-build clang <span class="nt">-c</span> proj01.c
</code></pre></div></div> <p>While reviewing the report, annotate the discovered bugs for further investigation.</p> <h3 id="step-2-enabling-additional-checkers">Step 2: Enabling Additional Checkers</h3> <p>Now, progressively enable the following checkers one at a time to uncover potential issues:</p> <ol> <li><code class="language-plaintext highlighter-rouge">optin.portability.UnixAPI</code>: Finds implementation-defined behavior in UNIX/Posix functions.</li> </ol> <p>To enable this checker, execute the following command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>scan-build <span class="nt">-enable-checker</span> optin.portability.UnixAPI clang <span class="nt">-c</span> proj01.c
</code></pre></div></div> <ol> <li><code class="language-plaintext highlighter-rouge">security.insecureAPI.strcpy</code>: Warn on uses of the ‘strcpy’ and ‘strcat’ functions.</li> </ol> <p>To enable this checker, use the following command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>scan-build <span class="nt">-enable-checker</span> security.insecureAPI.strcpy clang <span class="nt">-c</span> proj01.c
</code></pre></div></div> <ol> <li><code class="language-plaintext highlighter-rouge">security.insecureAPI.DeprecatedOrUnsafeBufferHandling</code>: Warn on uses of unsafe or deprecated buffer manipulating functions</li> </ol> <p>To enable this checker, execute the following command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>scan-build <span class="se">\</span>
<span class="nt">-enable-checker</span> security.insecureAPI.DeprecatedOrUnsafeBufferHandling <span class="se">\</span>
clang <span class="nt">-c</span> proj01.c
</code></pre></div></div> <h3 id="step-3-review-and-evaluate-reported-bugs">Step 3: Review and Evaluate Reported Bugs</h3> <p>After enabling each checker, carefully review the reported bugs. Determine whether these newly reported issues are true positives (actual problems) or false positives (incorrectly flagged). You can do this by examining the code and the specific checker warnings.</p> <h1 id="optional-continuous-integration-with-infer">Optional: Continuous Integration with Infer</h1> <p>Infer offers a significant advantage compared to other existing static analysis tools: it supports compositional analysis, allowing seamless integration into continuous integration (CI) pipelines. This integration provides developers with rapid feedback on their code. The goal of this exercise is to understand how Infer is currently applied in CI pipelines (<a href="https://fbinfer.com/docs/steps-for-ci">based on this tutorial</a>).</p> <h3 id="exercise-setup">Exercise Setup:</h3> <ol> <li>Begin by accessing the provided <code class="language-plaintext highlighter-rouge">exercise03.zip</code> file in the <code class="language-plaintext highlighter-rouge">lab-sa/exercise03</code> directory.</li> <li>In this exercise, we will utilise Infer to detect bugs in a feature branch of a toy project managed with Git. Follow these steps to run infer on two versions of the project and compare the results:</li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Navigate to the exercise directory</span>
<span class="nv">$ </span><span class="nb">cd </span>lab-sa/exercise03

<span class="c"># Unzip the exercise files</span>
<span class="nv">$ </span>unzip exercise03.zip

<span class="c"># Switch to the feature branch</span>
<span class="nv">$ </span>git checkout feature

<span class="c"># Obtain a list of changed files between feature and master branches</span>
<span class="nv">$ </span>git diff <span class="nt">--name-only</span> feature..master <span class="o">&gt;</span> index.txt

<span class="c"># Run Infer on the feature branch</span>
<span class="nv">$ </span>infer capture <span class="nt">--</span> clang <span class="nt">-c</span> main.c
<span class="nv">$ </span>infer analyze <span class="nt">--changed-files-index</span> index.txt

<span class="c"># Copy the Infer report for the feature branch</span>
<span class="nv">$ </span><span class="nb">cp </span>infer-out/report.json report-feature.json

<span class="c"># Switch to the master branch</span>
<span class="nv">$ </span>git checkout master

<span class="c"># Use the 'reactive' option to retain previously-captured files</span>
<span class="nv">$ </span>infer capture <span class="nt">--reactive</span> <span class="nt">--</span> clang <span class="nt">-c</span> main.c
<span class="nv">$ </span>infer analyze <span class="nt">--reactive</span> <span class="nt">--change-files-index</span> index.txt

<span class="c"># Compare the reports between feature and master branches</span>
<span class="nv">$ </span>infer reportdiff <span class="nt">--report-current</span> report-feature.json <span class="se">\</span>
<span class="nt">--report-previous</span> infer-out/report.json
</code></pre></div></div> <p>Upon running the last command, you will find three files in the <code class="language-plaintext highlighter-rouge">infer-out/differential</code> directory:</p> <ol> <li><code class="language-plaintext highlighter-rouge">introduces.json</code>: Contains issues found in the feature branch.</li> <li><code class="language-plaintext highlighter-rouge">fixed.json</code>: Contains issues found in the master branch but not in the feature branch.</li> <li><code class="language-plaintext highlighter-rouge">preexisting.json</code>: Contains issues found in both branches.</li> </ol> <h3 id="addressing-bugs">Addressing Bugs:</h3> <p>Next, follow these step to address the bugs introduced in the new feature branch:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Save the master branch's analysis report</span>
<span class="nv">$ </span><span class="nb">cp </span>infer-out/report.json report-master.json

<span class="c"># Return to the feature branch</span>
<span class="nv">$ </span>git checkout feature

<span class="c"># Edit 'main.c' to fix the bug ...</span>

<span class="c"># Re-analyse the project on the feature branch</span>
<span class="nv">$ </span>infer capture <span class="nt">--reactive</span> <span class="nt">--</span> clang <span class="nt">-c</span> main.c
<span class="nv">$ </span>infer analyze <span class="nt">--reactive</span> <span class="nt">--changed-files-index</span> index.txt

<span class="c"># Compare the reports</span>
<span class="nv">$ </span>infer reportdiff <span class="nt">--report-current</span> infer-out/report.json <span class="se">\</span>
<span class="nt">--report-previous</span> report-master.json
</code></pre></div></div> <p>Ensure that Infer does not produce any warnings, and the <code class="language-plaintext highlighter-rouge">introduces.json</code> file is empty indicating successful bug resolution.</p>]]></content><author><name></name></author><category term="code"/><summary type="html"><![CDATA[Prelude]]></summary></entry><entry><title type="html">Advances in Symbolic Execution: A Workshop on C, JavaScript, and WebAssembly</title><link href="https://www.filipeom.dev/blog/2022/sew/" rel="alternate" type="text/html" title="Advances in Symbolic Execution: A Workshop on C, JavaScript, and WebAssembly"/><published>2022-04-13T00:00:00+00:00</published><updated>2022-04-13T00:00:00+00:00</updated><id>https://www.filipeom.dev/blog/2022/sew</id><content type="html" xml:base="https://www.filipeom.dev/blog/2022/sew/"><![CDATA[<h1 id="program-schedule">Program Schedule</h1> <hr/> <h3 id="900-am---1030-am---symbolic-execution-for-webassembly-and-c">9:00 AM - 10:30 AM - Symbolic Execution for WebAssembly and C</h3> <ol> <li>20 mins - João Borges - Static Symbolic Execution</li> <li>20 mins - Filipe Marques - Concolic Execution</li> <li>20 mins - Francisco Sena - Parameterized Execution</li> </ol> <hr/> <h3 id="1100-am---1200-pm---summaries-in-symbolic-execution">11:00 AM - 12:00 PM - Summaries in Symbolic Execution</h3> <ol> <li>20 mins - Frederico Ramos - Summaries for Symbolic Execution</li> <li>20 mins - Manuel Costa - Symbolic Summaries for JavaScript</li> <li>20 mins - Rafael Gonçalves - Synthesizing Symbolic Summaries</li> </ol> <hr/> <h3 id="1200-pm---130-pm----lunch">12:00 PM - 1:30 PM - Lunch</h3> <hr/> <h3 id="130-pm---300-pm---symbolic-execution-for-javascript">1:30 PM - 3:00 PM - Symbolic Execution for JavaScript</h3> <ol> <li>20 mins - André Nascimento - ECMA-SL: An Intermediate Representation for JavaScript Analysis</li> <li>20 mins - Filipe Marques - Symbolic Execution for JavaScript</li> <li>20 mins - Miguel Monteiro - Explode.js: Identifying Security Vulnerabilities in Node.js Applications via Symbolic Execution</li> </ol> <hr/> <h3 id="300-pm---600-pm---symbolic-memory-models">3:00 PM - 6:00 PM - Symbolic Memory Models</h3> <ol> <li>20 mins - André Mendes - Symbolic Memory Models for C Language</li> <li>20 mins - Tutorial: Implementing 3 Symbolic Memory Models in While Language</li> </ol>]]></content><author><name></name></author><category term="workshops"/><summary type="html"><![CDATA[Program Schedule]]></summary></entry></feed>